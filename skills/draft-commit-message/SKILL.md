---
name: agent-infra-draft-commit-message
description: Use when the user asks for a commit message draft, or when work is complete and changes should be committed
---

Draft a commit message using the commit message template from claude.md.

Read the template before drafting. Then run git diff and git status to understand the changes. Output the draft as a code block so the user can edit it before committing.
